# Bench Harness Contract — frankenmermaid current status

**Owner:** frankenmermaid (Lane L, allocation addendum 2026-07-25 23:15Z).
**Contract:** [`CROSS_REPO_RECOMMENDATION_bench_harness_contract.md`](CROSS_REPO_RECOMMENDATION_bench_harness_contract.md),
campaign `perf-campaign-20260725` §2.
**Verified:** 2026-07-27 against the live frankenmermaid decision path and ledger preflight.

## Acceptance criteria

A mechanism counts only when it is present in the verdict-producing path:

- Part 1: the measured process reports the SHA-256 of its own executable.
- Part 2: each runtime emits an A/A null through the same paired routine and invocation as its
  measured arm.
- Part 3: the verdict gates on the bootstrap 95% CI of the median. CV and MAD are report-only.
- Competitive result: the actual legacy incumbent, mermaid-js, runs side-by-side with
  frankenmermaid in the same driver invocation.

## Current status

| Requirement | Current implementation | Status |
|---|---|:--:|
| Executing ELF SHA-256 | `crates/fm-cli/examples/headtohead.rs` reports `env::current_exe()`; the driver validates lowercase SHA-256 and positive byte count | ✅ |
| Same-invocation A/A | Rust emits `(default, default)`; Chromium emits `(mermaid, mermaid)` in the same browser invocation | ✅ |
| Median-CI verdict | The gate uses the larger per-runtime A/A bootstrap 95% CI radius with a 2× margin | ✅ |
| CV/MAD policy | Both are emitted as diagnostics with `cv_gate=never`; neither blocks a verdict | ✅ |
| Actual incumbent arm | Pinned mermaid-js `11.15.0` is driven beside frankenmermaid from one `run.mjs` invocation | ✅ |
| Ledger enforcement | Local hook and CI lint both split ledgers and enforce result class, ELF provenance, and incumbent evidence | ✅ |

## Current implementation

Part 1 lives in `crates/fm-cli/examples/headtohead.rs`: the runner hashes its own
`env::current_exe()` and emits it as its first record; the driver fails closed unless that record
contains a lowercase 64-hex SHA-256 and a positive ELF byte count. A hash computed by a shell step
*beside* the run proves nothing about which ELF executed — rch builds into an opaque per-worker
target dir.

Parts 2 and 3 live in the actual decision path:

- Rust calls one paired routine as `(default, default)` and then `(default, lean)`, with both arms
  back-to-back in each round, alternating order, the same batch, output checksums, and the median of
  per-round ratios.
- The browser runner emits its own `(mermaid, mermaid)` paired null from the same Chromium
  invocation. Since a Rust process and a JavaScript runtime cannot be arms in one binary, the
  cross-runtime driver conservatively uses the larger of the two per-engine A/A CI radii.
- The only blocking gate is
  `claim magnitude >= max(1.01, 1 + 2 * max(null CI radius))`. Records say
  `cv_gate=never`; CV and MAD are provenance only. A DNF has no point ratio and is explicitly
  `not_applicable`, never smuggled through a dispersion gate.

## Result classification

The performance ledger has exactly two kept-result classes:

- `maintenance-self-speedup`: frankenmermaid before versus after. It is maintenance, not campaign
  output, and does not support a public competitive claim.
- `incumbent-win`: frankenmermaid versus the pinned mermaid-js artifact in the same invocation. It
  is campaign output only when the row also carries the process-self-reported ELF, a measured A/A
  null, a shared invocation ID, and the measured ratio.

`scripts/ledger_preflight.mjs` parses both `docs/NEGATIVE_EVIDENCE.md` and
`docs/PERF_LEDGER.md`. It blocks a REJECT without an A/A null or counted mechanism, any kept result
without an exact class and executing-ELF self-report, and an `incumbent-win` without the pinned
same-invocation mermaid-js record.

## Lane L corpus admission (`bd-jil4`)

The current corpus contains 25 pinned items. Its four largest unmeasured workload boundaries are:

| item | workload boundary | generated bytes / revisions | pinned SHA-256 |
|---|---|---:|---|
| `er_schema_5000x8` | 5,000 ER nodes, eight attributes each | 901,655 / 1 | `f43a2b41e66146eb4eba7634eba367ba5667fe15c084a0e919d8e18480e46c56` |
| `er_schema_10000x8` | 10,000 ER nodes, eight attributes each | 1,806,655 / 1 | `185a597d70c58a4ab3abacff4454dd87a4aa662c602f97fb2a027e3c16d7b341` |
| `edit_trace_500x1000` | 1,001 successive live-preview documents | 23,016,535 / 1,001 | `4813beb88f379af9942bceaa10af37b6b7dc01c2210663697dba9e0312daa2cb` |
| `ci_batch_500` | 500 diagrams across five syntax families | 201,534 / 500 | `65b8f69a7b2ee114cfb2fb49557b34cbc7e2c15f1414a81b7d94215a46de432f` |

These are **construction results, not benchmark results**. Importing the generators produces all
25 items and all 25 hashes match `pins.json`. A measurement is admissible only through the current
head-to-head contract; the measurement window is tracked by `bd-ktx5`.

## Verification

Current conformance is checked by:

- deterministic bootstrap and gate self-tests in the live harness;
- regeneration of all 25 corpus hashes against `scripts/headtohead/pins.json`;
- `node scripts/ledger_preflight.mjs --self-test`;
- a fail-closed ledger lint over both split ledgers.

Re-verify when the verdict path, comparator pin, result-class schema, corpus generators, or either
ledger path changes.
